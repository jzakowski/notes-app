<project_name>
Simple TaskFlow - Minimalist To-Do List App
</project_name>

<overview>
Create a clean, minimalist to-do list application focused on simplicity and speed. Users should be able to add tasks, mark them as complete, delete them, and organize them into categories. No login required - all data stored locally in the browser for instant access. Perfect for personal daily task management.
</overview>

<technology_stack>
  <frontend>
    <framework>Next.js 16.x with React 19.x and TypeScript 5.5.x</framework>
    <styling>Tailwind CSS with clean, minimal design</styling>
    <state_management>React useState and localStorage for persistence</state_management>
    <routing>Next.js App Router - single page app with optional filters</routing>
    <forms>Native HTML form elements with React state</forms>
    <icons>Lucide React for clean iconography</icons>
    <animations>Simple CSS transitions for smooth interactions</animations>
    <images>Minimal image usage - focus on typography and icons</images>
    <database>localStorage for data persistence</database>
    <port>3000</port>
  </frontend>

  <backend>
    <runtime>None - pure client-side app</runtime>
    <database>localStorage API in browser</database>
    <validation>Basic form validation</validation>
    <typescript>TypeScript 5.5.x with strict mode enabled</typescript>
  </backend>

  <communication>
    <api>None - client-side only</api>
    <session>No sessions required</session>
  </communication>
</technology_stack>

<visual_design_inspiration>
  <minimal_characteristics>
    - Background: Clean white (#FFFFFF) with subtle gray accents (#F9FAFB)
    - Color Palette: Primary blue (#3B82F6), success green (#10B981), danger red (#EF4444), neutral grays
    - Typography: Clean sans-serif (Inter) throughout
    - UI Elements: Minimal borders, subtle shadows, rounded corners (8px)
    - Spacing: Generous white space, comfortable touch targets
    - Animation: Subtle transitions (150-200ms), no flashy effects
  </minimal_characteristics>

  <productivity_focused>
    - Fast keyboard shortcuts
    - Clear visual hierarchy
    - No distractions or clutter
    - Instant feedback on all actions
    - Mobile-friendly touch interactions
  </productivity_focused>

  <color_psychology>
    - Blue for primary actions (trust, focus)
    - Green for completion (success)
    - Red for deletion (warning)
    - Gray for secondary elements (calm)
    - White space for clarity (focus)
  </color_psychology>
</visual_design_inspiration>

<main_layout_design>
  <header_section>
    - App title: "TaskFlow"
    - Stats display: "X tasks, Y completed"
    - Progress bar showing completion percentage
    - Clean, minimal header with subtle border
  </header_section>

  <input_section>
    - Text input for new tasks
    - Category selector (Work, Personal, Shopping, Health, Other)
    - Add button (also works with Enter key)
    - Focus input on page load
  </input_section>

  <filter_section>
    - Filter tabs: All, Active, Completed
    - Category quick filters
    - Clear completed button
  </filter_section>

  <task_list>
    - Each task shows: checkbox, text, category badge, delete button
    - Completed tasks show strikethrough and dimmed
    - Hover effects on task items
    - Empty state with helpful message
  </task_list>
</main_layout_design>

## Database Schema (localStorage)

### Task Object Structure
```typescript
interface Task {
  id: string              // Unique ID (timestamp + random)
  text: string            // Task description
  category: Category      // Work, Personal, Shopping, Health, Other
  completed: boolean      // Completion status
  createdAt: number       // Timestamp
  completedAt?: number    // Completion timestamp
}

type Category = 'Work' | 'Personal' | 'Shopping' | 'Health' | 'Other'

interface TaskStore {
  tasks: Task[]
  lastUpdated: number
}
```

### localStorage Keys
- `taskflow_tasks` - Array of all tasks
- `taskflow_settings` - User preferences (optional)

## Design System

### Colors
```css
:root {
  --primary: #3B82F6;        /* Blue */
  --primary-hover: #2563EB;
  --success: #10B981;        /* Green */
  --danger: #EF4444;         /* Red */
  --gray-50: #F9FAFB;
  --gray-100: #F3F4F6;
  --gray-200: #E5E7EB;
  --gray-400: #9CA3AF;
  --gray-600: #4B5563;
  --gray-800: #1F2937;
  --white: #FFFFFF;
}
```

### Components

#### TaskItem
```tsx
<TaskItem>
  <Checkbox checked={completed} onChange={toggleTask} />
  <TaskText completed={completed}>{text}</TaskText>
  <CategoryBadge category={category} />
  <DeleteButton onClick={deleteTask} />
</TaskItem>
```

#### TaskInput
```tsx
<TaskInput>
  <TextInput placeholder="Add a new task..." />
  <CategorySelect />
  <AddButton>Add Task</AddButton>
</TaskInput>
```

#### FilterTabs
```tsx
<FilterTabs>
  <Tab active={filter === 'all'}>All</Tab>
  <Tab active={filter === 'active'}>Active</Tab>
  <Tab active={filter === 'completed'}>Completed</Tab>
</FilterTabs>
```

## Key Application Pages

### 1. Main Page (/)
**Features:**
- Add new tasks
- View all tasks with optional filters
- Mark tasks as complete/incomplete
- Delete tasks
- Clear all completed tasks
- Progress indicator

**Keyboard Shortcuts:**
- `Enter` - Add task (when input focused)
- `Escape` - Clear input
- `Cmd/Ctrl + Enter` - Toggle focused task

### 2. Filtered Views (Optional)
- `/active` - Show only active tasks
- `/completed` - Show only completed tasks

## State Management

### React State Structure
```typescript
const [tasks, setTasks] = useState<Task[]>([])
const [filter, setFilter] = useState<'all' | 'active' | 'completed'>('all')
const [categoryFilter, setCategoryFilter] = useState<Category | 'all'>('all')
const [inputValue, setInputValue] = useState('')
const [selectedCategory, setSelectedCategory] = useState<Category>('Other')

// Persist to localStorage on every change
useEffect(() => {
  localStorage.setItem('taskflow_tasks', JSON.stringify(tasks))
}, [tasks])

// Load from localStorage on mount
useEffect(() => {
  const stored = localStorage.getItem('taskflow_tasks')
  if (stored) {
    setTasks(JSON.parse(stored))
  }
}, [])
```

## Features (50 total)

### Core Features (10)
1. Add new task with text
2. Mark task as complete
3. Mark task as incomplete
4. Delete single task
5. Clear all completed tasks
6. View all tasks
7. View only active tasks
8. View only completed tasks
9. Task counter display
10. Progress bar

### Categories (5)
11. Work category
12. Personal category
13. Shopping category
14. Health category
15. Other category

### Category Features (5)
16. Select category when adding task
17. Filter by single category
18. Show category badge on task
19. Category color coding
20. Reset category filter

### UX Features (10)
21. Empty state message
22. Confirmation for delete (optional)
23. Focus input on load
24. Smooth transitions
25. Hover effects
26. Mobile responsive
27. Touch-friendly buttons
28. Keyboard shortcuts
29. Auto-save to localStorage
30. Persist across sessions

### Polish (10)
31. Timestamp display
32. Task count by status
33. Animated checkbox
34. Delete animation
35. Add animation
36. Loading state
37. Error handling
38. Accessibility (ARIA labels)
39. Focus management
40. Keyboard navigation

### Extra Polish (10)
41. Dark mode support
42. Task search/filter
43. Sort by date
44. Sort by completion
45. Bulk actions
46. Export tasks
47. Import tasks
48. Undo last action
49. Task editing
50. Due dates (optional)

## File Structure

```
src/
├── app/
│   ├── layout.tsx          // Root layout
│   ├── page.tsx            // Main to-do page
│   └── globals.css         // Global styles
├── components/
│   ├── TaskItem.tsx        // Single task component
│   ├── TaskInput.tsx       // Input form
│   ├── FilterTabs.tsx      // Filter buttons
│   ├── ProgressBar.tsx     // Progress indicator
│   └── EmptyState.tsx      // Empty state message
├── lib/
│   ├── storage.ts          // localStorage helpers
│   └── types.ts            // TypeScript types
└── hooks/
    ├── useTasks.ts         // Task state management
    └── useKeyboard.ts      // Keyboard shortcuts
```

## Development Scripts

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit"
  }
}
```

## Launch Checklist

### MVP (20 features)
- [ ] Add/delete tasks
- [ ] Mark complete/incomplete
- [ ] Basic filters (all/active/completed)
- [ ] Categories
- [ ] localStorage persistence
- [ ] Progress bar
- [ ] Task counter
- [ ] Empty state
- [ ] Mobile responsive
- [ ] Basic animations

### Polish (15 features)
- [ ] Keyboard shortcuts
- [ ] Category filters
- [ ] Category badges
- [ ] Timestamp display
- [ ] Smooth transitions
- [ ] Dark mode
- [ ] Task editing
- [ ] Undo action
- [ ] Search/filter
- [ ] Sort options

### Advanced (15 features)
- [ ] Bulk actions
- [ ] Export/import
- [ ] Due dates
- [ ] Subtasks
- [ ] Task priority
- [ ] Drag & drop reorder
- [ ] Tags/labels
- [ ] Reminders
- [ ] Statistics
- [ ] Archive

## Success Metrics

### Technical
- Page load: < 1 second
- First paint: < 500ms
- Bundle size: < 50KB
- Zero runtime errors

### UX
- Add task: < 2 seconds
- Mark complete: < 100ms (instant feel)
- Delete task: < 100ms (instant feel)
- Switch filter: < 50ms
